//Check if a number is divisible by another number.
// modulus-divisible.js
let a = 45, b = 9;
if (a % b === 0) {
    console.log(`${a} is divisible by ${b}`);
} else {
    console.log(`${a} is not divisible by ${b}`);
}
