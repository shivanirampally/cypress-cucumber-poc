//How would you update the total test cases when new ones are added?

let regressionSuite = 120;
let newTests = 15;
regressionSuite += newTests; 
console.log(`Updated Regression Suite Count: ${regressionSuite}`);
